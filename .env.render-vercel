# ========================================
# VERCEL FRONTEND + RENDER BACKEND
# ========================================

# ------------------
# FRONTEND (Vercel)
# ------------------
# Add these in Vercel Dashboard > Settings > Environment Variables

NEXT_PUBLIC_API_URL=https://your-app-name.onrender.com
NEXT_PUBLIC_WS_URL=https://your-app-name.onrender.com
NODE_ENV=production

# ------------------
# BACKEND (Render)
# ------------------
# Add these in Render Dashboard > Environment tab

# Database (Render will auto-provide if you add PostgreSQL database)
DATABASE_URL=postgresql://user:password@hostname:port/database

# Redis (Use Upstash or external Redis)
REDIS_URL=rediss://default:password@hostname:port

# JWT (generate with: openssl rand -base64 32)
JWT_SECRET=your-production-secret-change-this

# Server (Render uses PORT from environment)
PORT=10000
NODE_ENV=production

# CORS (allow your Vercel domains)
CORS_ORIGINS=https://your-app.vercel.app,https://your-app-*.vercel.app

# H3 Config
H3_RESOLUTION=8

# ------------------
# DEPLOYMENT STEPS
# ------------------
# 1. Create Render account
# 2. Create PostgreSQL database (Free tier available)
#    - Copy DATABASE_URL from Render dashboard
# 3. Create Upstash Redis account (Free tier: 10k commands/day)
#    - Copy REDIS_URL from Upstash console
# 4. Create Web Service on Render
#    - Build Command: cd backend && npm install && npm run build
#    - Start Command: cd backend && npm run start:prod
#    - Add environment variables above
# 5. Create Vercel account and import frontend
# 6. Add NEXT_PUBLIC_API_URL with your Render URL
# 7. Deploy on Vercel
#
# NOTE: Render free tier may spin down after inactivity
