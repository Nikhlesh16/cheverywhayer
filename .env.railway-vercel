# ========================================
# VERCEL FRONTEND + RAILWAY BACKEND
# ========================================

# ------------------
# FRONTEND (Vercel)
# ------------------
# Add these in Vercel Dashboard > Settings > Environment Variables

NEXT_PUBLIC_API_URL=https://your-app-name.up.railway.app
NEXT_PUBLIC_WS_URL=https://your-app-name.up.railway.app
NODE_ENV=production

# ------------------
# BACKEND (Railway)
# ------------------
# Add these in Railway Dashboard > Variables tab

# Database (Railway will auto-provide if you add PostgreSQL plugin)
DATABASE_URL=${{Postgres.DATABASE_URL}}

# Redis (Railway will auto-provide if you add Redis plugin)
REDIS_URL=${{Redis.REDIS_URL}}

# JWT (generate with: openssl rand -base64 32)
JWT_SECRET=your-production-secret-change-this

# Server
PORT=3001
NODE_ENV=production

# CORS (allow your Vercel domains)
CORS_ORIGINS=https://your-app.vercel.app,https://your-app-*.vercel.app

# H3 Config
H3_RESOLUTION=8

# ------------------
# DEPLOYMENT STEPS
# ------------------
# 1. Create Railway account and new project
# 2. Add PostgreSQL plugin (DATABASE_URL auto-configured)
# 3. Add Redis plugin (REDIS_URL auto-configured)
# 4. Deploy backend: railway up (or connect GitHub repo)
# 5. Note the Railway URL: https://your-app-name.up.railway.app
# 6. Create Vercel account and import frontend
# 7. Add NEXT_PUBLIC_API_URL in Vercel environment variables
# 8. Deploy on Vercel
