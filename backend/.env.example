# ==================================
# DATABASE CONFIGURATION
# ==================================
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
# Local: postgresql://postgres:postgres@localhost:5432/hyperlocal_db
# Supabase: postgresql://postgres:[password]@db.[project].supabase.co:5432/postgres
# Railway/Render: Provided by platform
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/hyperlocal_db

# ==================================
# REDIS CONFIGURATION
# ==================================
# Option 1: Use Redis URL (recommended for cloud - Upstash, Railway, Render)
# Format: redis://default:[password]@[host]:[port]
# Upstash: rediss://default:[password]@[region].upstash.io:[port] (note: rediss with double 's' for TLS)
REDIS_URL=

# Option 2: Individual Redis config (for local Docker/development)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# ==================================
# JWT AUTHENTICATION
# ==================================
# Secret key for JWT signing (CHANGE IN PRODUCTION!)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRATION=7d

# ==================================
# SERVER CONFIGURATION
# ==================================
# Port for the backend server (cloud platforms may override this)
PORT=3001

# Node environment: development, production, test
NODE_ENV=development

# ==================================
# CORS & FRONTEND CONFIGURATION
# ==================================
# Comma-separated list of allowed origins for CORS
# Use * for all origins (not recommended for production)
# Examples:
# - Local: http://localhost:3000
# - Vercel: https://your-app.vercel.app,https://your-app-*.vercel.app
# - Netlify: https://your-app.netlify.app
# - Multiple: https://app.example.com,https://www.example.com
CORS_ORIGINS=http://localhost:3000

# ==================================
# FILE STORAGE (Optional)
# ==================================
# For file uploads - can use local storage or cloud storage
# S3_BUCKET=your-bucket-name
# S3_REGION=us-east-1
# S3_ACCESS_KEY_ID=your-access-key
# S3_SECRET_ACCESS_KEY=your-secret-key
# CLOUDINARY_URL=cloudinary://api_key:api_secret@cloud_name

# ==================================
# H3 GEOSPATIAL CONFIGURATION
# ==================================
# H3 resolution (8 = ~5km hexagons, 9 = ~1.2km, 7 = ~22km)
H3_RESOLUTION=8

# ==================================
# DEPLOYMENT NOTES
# ==================================
# Local Docker: Use the defaults above
# Railway: Add DATABASE_URL and REDIS_URL from Railway dashboard
# Render: Add DATABASE_URL and REDIS_URL from Render dashboard
# Fly.io: Configure via fly.toml and secrets
# Supabase DB: Get DATABASE_URL from Supabase project settings
# Upstash Redis: Get REDIS_URL from Upstash console (use rediss:// for TLS)
